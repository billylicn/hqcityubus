# 贡献指南

感谢你帮助维护通琴号班次查询。

## 修正班次

1. 阅读 `data/README.md`。
2. 只修改 `data/` 中的源文件，不要手工修改生成结果。
3. 运行 `npm run data:generate`。
4. 同时提交源数据和 `src/data/routes.generated.json`，让审阅者能看到解析结果。
5. 在 Pull Request 中说明数据来源和生效日期；如有公开通知，附上出处。

## 修改代码

- 保持网站为纯静态构建，不引入必须在线运行的后端。
- 不在仓库中提交密钥、账号、部署凭据或个人信息。
- 优先保证移动端、键盘操作、无障碍语义和深色模式可用。
- 提交前运行：

```bash
npm run check
```

## 报告问题

请提供方向、线路名称、当前显示内容、预期内容，以及可以核实的班次来源。不要在 Issue 中发布个人通勤记录或其他敏感信息。
